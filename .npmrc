# Build script permissions are managed via pnpm-workspace.yaml (allowBuilds).
# Only explicitly approved packages may run lifecycle scripts; all others are blocked.
# This is the pnpm 11 canonical supply-chain hardening approach.
node-linker=hoisted
public-hoist-pattern[]=*
